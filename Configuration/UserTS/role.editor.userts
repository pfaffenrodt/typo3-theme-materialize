#
# Insert into Editor User TSconfig
# <INCLUDE_TYPOSCRIPT: source="FILE:EXT:materialize/Configuration/UserTS/role.editor.userts">
#
options {
    # Allow the editor to clear the page cache
    clearCache.pages = 1
    # Allow the editor to clear all caches
    clearCache.all = 1
}